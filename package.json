{
  "name": "nodefirst",
  "version": "1.0.0",
  "description": "<!-- #Importants -->\r global - no window\r __dirname = path of current directory\r __filename = file name\r require =  function to use module(commonjs)\r module = information about current module(file)\r process  = info about env wher the program being execute",
  "main": "osModles.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
